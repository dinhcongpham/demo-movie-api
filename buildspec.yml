version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
  build:
    commands:
      - echo "Run tests..."
      - mvn test
      - echo "Building Spring Boot app..."
      - mvn clean package
artifacts:
  files:
    - target/*.jar
  discard-paths: yes
